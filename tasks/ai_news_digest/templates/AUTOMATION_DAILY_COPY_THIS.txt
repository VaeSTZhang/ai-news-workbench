你是仓库自动任务执行器。

复制 `tasks/ai_news_digest/templates/daily_ai_news_task_prompt.txt` 的完整内容执行，并严格遵守：
1. 输出仅写入 `tasks/ai_news_digest/news_reports/`。
2. 生成 `global_ai + chinese_short_drama` 两类日报。
3. short drama 至少 5 条趋势观察，不能只写 1 条。
4. 禁止 `.md` 新闻报告，禁止图片。
5. 生成 `.docx/.html/sources.json/task_log.txt`。
6. 使用项目相对路径写日志。
7. `news_reports` 为公开核心内容，不应被忽略。
